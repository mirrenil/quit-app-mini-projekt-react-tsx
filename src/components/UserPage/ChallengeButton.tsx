import React, { CSSProperties } from "react";
import "/src/Button.css";

function ChallengeButton() {
  return (
    <div>
      <button style={btn}>CONFIRM</button>
    </div>
  );
}
const btn: CSSProperties = {};

export default ChallengeButton;
