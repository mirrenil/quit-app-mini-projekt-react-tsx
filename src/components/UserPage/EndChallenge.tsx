import React, { CSSProperties } from "react";

function EndChallenge() {
  return (
    <div>
      <button style={btn}>END CHALLENGE</button>
    </div>
  );
}
const btn: CSSProperties = {};

export default EndChallenge;
